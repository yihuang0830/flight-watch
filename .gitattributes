# 历史 CSV 是纯追加的：本地和 CI 各自会往同一天的文件里追加不同轮次的记录。
# union 合并策略直接取两边并集，避免每次都手工解冲突。
# （两边表头内容相同，不会产生重复表头。）
history/*.csv merge=union

# 这两个是派生产物，冲突时保留检出的那份即可，下一轮会重新生成。
flights.db   -diff
docs/index.html -diff
